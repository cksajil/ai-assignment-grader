Place your demo GIF or screenshot here as `demo.gif` or `demo.png`.

Record a 60-second screen capture of the Gradio app:
1. Upload the sample files from sample_data/
2. Click Grade Assignment
3. Show the live timer ticking
4. Scroll through the results report

Then convert to GIF using:
  ffmpeg -i demo.mp4 -vf "fps=10,scale=800:-1" demo.gif

Or use https://ezgif.com/video-to-gif for a quick online conversion.
